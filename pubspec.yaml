name: mylk
description: This is mylk
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^0.1.3
  font_awesome_flutter: ^8.2.0
  intl: ^0.16.1
  sqflite: ^1.1.0
  path_provider: ^0.5.0+1
  unicorndial: ^1.1.5
  linked_scroll_controller: ^0.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/images/