import 'package:flutter/material.dart';

class JournalsScreen extends StatelessWidget {
  const JournalsScreen({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Woop woop"),
    );
  }
}
